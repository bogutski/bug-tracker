import React from 'react';

const CreateBoard = () => {
    return (
        <div>
            <button type="button" className="btn btn-link">Create Board</button>
        </div>
    );
};

export default CreateBoard;
